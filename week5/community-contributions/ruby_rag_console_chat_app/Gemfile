# frozen_string_literal: true

source "https://rubygems.org"

gem 'ruby-openai'
gem 'chroma-db', github: 'koic/chroma', branch: 'support_chroma_v2_api' # Using fork because original one comes with outdated API
gem 'pry-byebug'
gem 'ruby-next' # to auto-transpile the required files from source on load. Otherwise the code from chroma-db may not work correctly
gem 'pragmatic_segmenter'