# Bug #44

**Level:** hard  
**Description:** implements Dijkstra's shortest path algorithm  
**Bug Types:** NameError, LogicError  
**Bug Count:** 2  
**Tags:** graph, greedy  
**Created:** 2025-09-06T05:20:14  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def dijkstra(graph, start):
    import heapq
    priority_queue = [(0, start)]
    distances = {node: float('inf') for node in graph}
    distances[start] = 0
    while priority_queue:
        current_distance, current_node = heapq.heappop(priority_queue)
        if current_distance > distances[current_node]:
            continue
        for neighbor, weight in graph[current_node].items():
            distance = current_distance + weight
            if distance < distances[neighbor]:
                distances[neighbor] = distance
                heapq.heappush(priority_queue, (distance, neighbor))
    return distances
```

## Buggy Code

```python
def dijkstra(graph, start):
    import heapq
    priority_queue = [(0, start)]
    distances = {node: float('inf') for node in grap}
    distances[start] = 0
    while priority_queue:
        current_distance, current_node = heapq.heappop(priority_queue)
        if current_distance > distances[current_node]:
            continue
        for neighbor, weight in graph[current_node].items():
            distance = current_distance + weight
            if distance < distances[neighbor]:
                distances[neighbor] = distance
                heapq.heappush(priority_queue, (distance, neighbor))
    return disances
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | NameError | Changed 'graph' to 'grap', causing a NameError when attempting to access the graph variable. |
| 2 | 15 | LogicError | Changed 'disances' to 'disances', which was intended to be 'distances', leading to returning an incorrect variable. |
