# Bug #11

**Level:** easy  
**Description:** calculates the average of a list of numbers  
**Bug Types:** NameError, LogicError  
**Bug Count:** 2  
**Tags:** math, array  
**Created:** 2025-03-12T09:57:19  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def calculate_average(numbers):
    if not numbers:
        return 0
    total_sum = sum(numbers)
    count = len(numbers)
    return total_sum / count
```

## Buggy Code

```python
def calculate_average(numbers):
    if not numbers:
        return 0
    total_sum = sum(numers)
    count = len(numbers) - 1
    return total_sum / count
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | NameError | Typo in 'numers' instead of 'numbers' causes a NameError. |
| 2 | 5 | LogicError | Subtracting 1 from 'count' causes the average calculation to be incorrect. |
