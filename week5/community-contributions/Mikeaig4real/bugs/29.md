# Bug #29

**Level:** medium  
**Description:** counts the frequency of each character in a string  
**Bug Types:** IndexError, IndentationError  
**Bug Count:** 2  
**Tags:** string, hash-table  
**Created:** 2025-06-23T19:29:10  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def count_character_frequency(input_string):
    frequency = {}
    for char in input_string:
        if char in frequency:
            frequency[char] += 1
        else:
            frequency[char] = 1
    return frequency

# Example usage:
# print(count_character_frequency("hello world"))
```

## Buggy Code

```python
def count_character_frequency(input_string):
    frequency = {}
    for char in input_string:
        if char in frequncy:
            frequency[char] += 1
        else:
            frequency[char] = 1
    return frequency

# Example usage:
# print(count_character_frequency("hello world"))
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 5 | IndexError | Typo in variable name 'frequncy' causes it to be undefined. |
| 2 | 2 | IndentationError | Incorrect indentation level on the line defining 'frequency'. |
