# Bug #7

**Level:** easy  
**Description:** reverses a string  
**Bug Types:** NameError, IndentationError  
**Bug Count:** 2  
**Tags:** string, error-handling  
**Created:** 2025-02-17T20:44:34  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def reverse_string(input_string):
    if input_string is None:
        return None
    if not isinstance(input_string, str):
        raise ValueError("Input must be a string")
    return input_string[::-1]
```

## Buggy Code

```python
def reverse_string(input_string):
    if input_string is None:
        return None
    if not isinstance(input_strng, str):
        raise ValueError("Input must be a string")
    return input_string[::-1]
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | NameError | Changed input_string to input_strng, causing a NameError since input_strng is not defined. |
| 2 | 5 | IndentationError | Misplaced indentation on the return statement, which disrupts the structure of the function. |
