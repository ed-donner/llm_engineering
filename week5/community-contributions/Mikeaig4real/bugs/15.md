# Bug #15

**Level:** easy  
**Description:** joins a list of strings into a single string  
**Bug Types:** NameError  
**Bug Count:** 1  
**Tags:** string, array  
**Created:** 2025-04-02T13:42:37  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def join_strings(string_list, separator=' '):
    if not isinstance(string_list, list):
        raise ValueError("Input must be a list of strings.")
    if not all(isinstance(s, str) for s in string_list):
        raise ValueError("All elements in the list must be strings.")
    return separator.join(string_list) if string_list else ''
```

## Buggy Code

```python
def join_strings(string_list, separator=' '):
    if not isinstance(string_lists, list):
        raise ValueError("Input must be a list of strings.")
    if not all(isinstance(s, str) for s in string_list):
        raise ValueError("All elements in the list must be strings.")
    return separator.join(string_list) if string_list else ''
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 3 | NameError | Changed 'string_list' to 'string_lists', causing a NameError due to the undefined variable. |
