# Bug #60

**Level:** hard  
**Description:** calculates the edit distance between two strings using dynamic programming  
**Bug Types:** IndentationError, IndentationError  
**Bug Count:** 2  
**Tags:** string, dynamic-programming  
**Created:** 2025-12-26T20:13:40  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def edit_distance(str1, str2):
    len_str1, len_str2 = len(str1), len(str2)
    dp = [[0] * (len_str2 + 1) for _ in range(len_str1 + 1)]
    
    for i in range(len_str1 + 1):
        for j in range(len_str2 + 1):
            if i == 0:
                dp[i][j] = j
            elif j == 0:
                dp[i][j] = i
            elif str1[i - 1] == str2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1]
            else:
                dp[i][j] = 1 + min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1])
    
    return dp[len_str1][len_str2]
```

## Buggy Code

```python
def edit_distance(str1, str2):
    len_str1, len_str2 = len(str1), len(str2)
    dp = [[0] * (len_str2 + 1) for _ in range(len_str1 + 1)]
    
    for i in range(len_str1 + 1):
        for j in range(len_str2 + 1):
            if i == 0:
                dp[i][j] = j
            elif j == 0:
                dp[i][j] = i
            elif str1[i - 1] == str2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1]
            else:
                dp[i][j] = 1 + min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1])

    return dp[len_str1][len_str2]
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 3 | IndentationError | The line with 'for i in range(len_str1 + 1):' is improperly indented. |
| 2 | 8 | IndentationError | The line with 'return dp[len_str1][len_str2]' is improperly indented. |
