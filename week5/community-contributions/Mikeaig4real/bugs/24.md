# Bug #24

**Level:** medium  
**Description:** finds the shortest path in a grid using breadth-first search (BFS)  
**Bug Types:** IndentationError, IndexError, SyntaxError  
**Bug Count:** 3  
**Tags:** graph, queue  
**Created:** 2025-06-06T22:31:09  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def shortest_path(grid, start, end):
    if not grid or not grid[0] or start == end:
        return 0
    
    rows, cols = len(grid), len(grid[0])
    queue = [(start, 0)]
    visited = set()
    visited.add(start)

    directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
    
    while queue:
        (current_x, current_y), distance = queue.pop(0)
        if (current_x, current_y) == end:
            return distance
        
        for dx, dy in directions:
            new_x, new_y = current_x + dx, current_y + dy
            if 0 <= new_x < rows and 0 <= new_y < cols and grid[new_x][new_y] == 0 and (new_x, new_y) not in visited:
                visited.add((new_x, new_y))
                queue.append(((new_x, new_y), distance + 1))
    
    return -1
```

## Buggy Code

```python
def shortest_path(grid, start, end):
    if not grid or not grid[0] or start == end:
        return 0
    
    rows, cols = len(grid), len(grid[0])
    queue = [(start, 0)]
    visited = set()
    visited.add(start)

    directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
    
    while queue:
      (current_x, current_y), distance = queue.pop(0)
        if (current_x, current_y) == end:
            return distance
        
        for dx, dy in directions:
            new_x, new_y = current_x + dx, current_y + dy
            if 0 <= new_x < rows and 0 <= new_y <= cols and grid[new_x][new_y] == 0 and (new_x, new_y) not in visited:
                visited.add((new_x, new_y))
                queue.append(((new_x, new_y), distance + 1))
    
    return -1
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 10 | IndentationError | Changed indentation of the line to be less indented, causing a misalignment in the control structure. |
| 2 | 15 | IndexError | Changed the condition from '<' to '<=' for new_y, which could lead to an index out of bounds error when accessing grid[new_x][new_y]. |
| 3 | 5 | SyntaxError | Missed a colon at the end of the if statement, causing a syntax error. |
