# Bug #6

**Level:** easy  
**Description:** counts the number of vowels in a string  
**Bug Types:** NameError, LogicError  
**Bug Count:** 2  
**Tags:** string, counting  
**Created:** 2025-02-09T01:16:33  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def count_vowels(input_string):
    vowels = "aeiouAEIOU"
    count = 0
    for char in input_string:
        if char in vowels:
            count += 1
    return count
```

## Buggy Code

```python
def count_vowels(input_string):
    vowles = "aeiouAEIOU"
    count = 0
    for char in input_string:
        if char in vowles:
            count += 1
    return count
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 2 | NameError | The variable 'vowels' is misspelled as 'vowles', leading to a NameError when trying to access it. |
| 2 | 5 | LogicError | The logic to count vowels is flawed due to the misspelled variable, resulting in incorrect counts. |
