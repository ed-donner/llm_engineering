# Bug #2

**Level:** easy  
**Description:** creates a new list with squared values  
**Bug Types:** NameError  
**Bug Count:** 1  
**Tags:** array, math  
**Created:** 2025-01-25T22:30:02  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def square_values(input_list):
    if not isinstance(input_list, list):
        raise ValueError("Input must be a list.")
    return [x**2 for x in input_list if isinstance(x, (int, float))]
```

## Buggy Code

```python
def square_values(input_list):
    if not isinstance(input_list, lst):
        raise ValueError("Input must be a list.")
    return [x**2 for x in input_list if isinstance(x, (int, float))]
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 2 | NameError | Changed 'list' to 'lst', which is undefined, causing a NameError. |
