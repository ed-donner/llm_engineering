# Bug #10

**Level:** easy  
**Description:** retrieves the first element of a list  
**Bug Types:** SyntaxError, LogicError, IndexError  
**Bug Count:** 3  
**Tags:** array, list  
**Created:** 2025-02-27T17:37:46  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def get_first_element(input_list):
    if not isinstance(input_list, list):
        raise ValueError("Input must be a list.")
    if len(input_list) == 0:
        return None
    return input_list[0]
```

## Buggy Code

```python
def get_first_element(input_list):
    if not isinstance(input_list, list)
        raise ValueError("Input must be a list.")
    if len(input_list) == 1:
        return None
    return input_list[1]
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 2 | SyntaxError | Missing a colon at the end of the if statement. |
| 2 | 4 | LogicError | Changed comparison from len(input_list) == 0 to len(input_list) == 1, which causes incorrect handling of empty lists. |
| 3 | 6 | IndexError | Changed the return index from 0 to 1, which will cause an error when accessing the first element of the list. |
