# Bug #43

**Level:** hard  
**Description:** calculates the factorial of a number  
**Bug Types:** NameError  
**Bug Count:** 1  
**Tags:** math, algorithm  
**Created:** 2025-09-04T12:03:20  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def calculate_factorial(n):
    if not isinstance(n, int) or n < 0:
        raise ValueError("Input must be a non-negative integer.")
    result = 1
    for i in range(2, n + 1):
        result *= i
    return result
```

## Buggy Code

```python
def calculate_factorial(n):
    if not isinstance(n, int) or n < 0:
        raise ValueError("Input must be a non-negative integer.")
    result = 1
    for i in range(2, n + 1):
        results *= i
    return result
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 6 | NameError | Changed 'result' to 'results' causing a NameError since 'results' is not defined. |
