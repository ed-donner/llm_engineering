# Bug #59

**Level:** hard  
**Description:** determines the shortest path between all pairs of vertices using the Floyd-Warshall algorithm  
**Bug Types:** NameError, IndentationError, SyntaxError  
**Bug Count:** 3  
**Tags:** graph, dynamic-programming  
**Created:** 2025-12-25T00:07:03  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def floyd_warshall(graph):
    num_vertices = len(graph)
    dist = [[float('inf')] * num_vertices for _ in range(num_vertices)]
    
    for i in range(num_vertices):
        for j in range(num_vertices):
            if i == j:
                dist[i][j] = 0
            elif graph[i][j] != 0:
                dist[i][j] = graph[i][j]

    for k in range(num_vertices):
        for i in range(num_vertices):
            for j in range(num_vertices):
                dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])
    
    return dist
```

## Buggy Code

```python
def floyd_warshall(graph):
    num_vertices = len(graph)
    dist = [[float('inf')] * num_vertices for _ in range(num_vertices)]
    
    for i in range(num_vertices):
        for j in range(num_vertices):
            if i == j:
                dist[i][j] = 0
            elif graph[i][j] != 0:
                dist[i][j] = graph[i][j]

    for k in range(num_vertex):
        for i in range(num_vertices):
            for j in range(num_vertices):
                dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])
    
    return dist
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 12 | NameError | Changed 'num_vertices' to 'num_vertex', causing a NameError. |
| 2 | 6 | IndentationError | Incorrect indentation level for the 'for k in range(num_vertex):' line. |
| 3 | 1 | SyntaxError | Missing colon after 'def floyd_warshall(graph)'. |
