# Bug #27

**Level:** medium  
**Description:** sorts an array of numbers using bubble sort algorithm  
**Bug Types:** LogicError, TypeError  
**Bug Count:** 2  
**Tags:** sorting, array  
**Created:** 2025-06-21T14:11:13  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        swapped = False
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        if not swapped:
            break
    return arr
```

## Buggy Code

```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        swapped = true
        for j in range(0, n - i - 2):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        if not swapped:
            break
    return arr
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | LogicError | Changed 'n - i - 1' to 'n - i - 2', which causes the loop to miss the last comparison for sorting. |
| 2 | 3 | TypeError | Changed 'False' to 'true', which causes a NameError since 'true' is not defined in Python. |
