# Bug #14

**Level:** easy  
**Description:** counts the number of sentences in a string  
**Bug Types:** LogicError, IndentationError  
**Bug Count:** 2  
**Tags:** string, array  
**Created:** 2025-03-22T18:44:30  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def count_sentences(text):
    if not text:
        return 0
    sentences = text.split('.')
    sentences = [s for s in sentences if s.strip()]
    return len(sentences)
```

## Buggy Code

```python
def count_sentences(text):
    if not text:
        return 0
    sentence = text.split('.')
    sentences = [s for s in sentences if s.strip()]
    return len(sentences)
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | LogicError | Changed variable 'sentences' to 'sentence', which breaks the logic of filtering. |
| 2 | 3 | IndentationError | Incorrect indentation of the return statement. |
