# Bug #20

**Level:** easy  
**Description:** formats a number with two decimal places  
**Bug Types:** TypeError  
**Bug Count:** 1  
**Tags:** math, string  
**Created:** 2025-05-11T19:45:31  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def format_number_to_two_decimal_places(value):
    try:
        formatted_value = f"{float(value):,.2f}"
        return formatted_value
    except (ValueError, TypeError):
        return "Invalid input"
```

## Buggy Code

```python
def format_number_to_two_decimal_places(value):
    try:
        formatted_value = f"{value:,.2f}"
        return formatted_value
    except (ValueError, TypeError):
        return "Invalid input"
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | TypeError | Changed 'float(value)' to 'value', which can lead to a TypeError if 'value' is not a number. |
