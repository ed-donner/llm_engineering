# Bug #4

**Level:** easy  
**Description:** counts the number of characters in a string  
**Bug Types:** NameError, TypeError  
**Bug Count:** 2  
**Tags:** string, error-handling  
**Created:** 2025-01-31T00:28:19  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def count_characters(input_string):
    if not isinstance(input_string, str):
        raise ValueError("Input must be a string")
    return len(input_string)
```

## Buggy Code

```python
def count_characters(input_str):
    if not isinstance(input_str, str):
        raise ValueError("Input must be a string")
    return len(input_string)
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 1 | NameError | Changed variable name from 'input_string' to 'input_str', leading to a NameError when trying to access 'input_string'. |
| 2 | 4 | TypeError | Using 'len(input_string)' instead of 'len(input_str)' results in a TypeError because 'input_string' is not defined. |
