# Bug #57

**Level:** hard  
**Description:** solves the knapsack problem using dynamic programming  
**Bug Types:** IndentationError  
**Bug Count:** 1  
**Tags:** dynamic-programming, array  
**Created:** 2025-12-17T07:04:49  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def knapsack(weights, values, capacity):
    n = len(values)
    dp = [[0] * (capacity + 1) for _ in range(n + 1)]
    
    for i in range(1, n + 1):
        for w in range(capacity + 1):
            if weights[i - 1] <= w:
                dp[i][w] = max(dp[i - 1][w], dp[i - 1][w - weights[i - 1]] + values[i - 1])
            else:
                dp[i][w] = dp[i - 1][w]
    
    return dp[n][capacity]
```

## Buggy Code

```python
def knapsack(weights, values, capacity):
    n = len(values)
    dp = [[0] * (capacity + 1) for _ in range(n + 1)]
    
    for i in range(1, n + 1):
        for w in range(capacity + 1):
            if weights[i - 1] <= w:
                dp[i][w] = max(dp[i - 1][w], dp[i - 1][w - weights[i - 1]] + values[i - 1])
            else:
            dp[i][w] = dp[i - 1][w]
    
    return dp[n][capacity]
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 10 | IndentationError | The 'else' statement is not properly indented, which breaks the expected structure of the code. |
