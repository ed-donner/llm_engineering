# Bug #49

**Level:** hard  
**Description:** finds the maximum subarray sum using Kadane's algorithm  
**Bug Types:** IndexError, NameError, IndentationError  
**Bug Count:** 3  
**Tags:** array, dynamic-programming  
**Created:** 2025-09-25T15:41:48  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def max_subarray_sum(arr):
    if not arr:
        return 0
    current_sum = max_sum = arr[0]
    for num in arr[1:]:
        current_sum = max(num, current_sum + num)
        max_sum = max(max_sum, current_sum)
    return max_sum
```

## Buggy Code

```python
def max_subarray_sum(arr):
    if not arr:
        return 0
    current_sum = max_sum = arr[1]
    for num in arr[1:]:
        current_sum = max(num, current_sum + num)
        max_sum = max(max_sum, currentsum)
    return max_sum
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | IndexError | Changed arr[0] to arr[1], which can cause an IndexError if arr has only one element. |
| 2 | 7 | NameError | Changed variable 'current_sum' to 'currentsum', which leads to a NameError because 'currentsum' is undefined. |
| 3 | 5 | IndentationError | Incorrectly indented the line 'for num in arr[1:]' which leads to an IndentationError. |
