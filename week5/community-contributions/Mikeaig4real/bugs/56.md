# Bug #56

**Level:** hard  
**Description:** implements the A* search algorithm for pathfinding in a grid  
**Bug Types:** NameError, IndentationError, IndexError  
**Bug Count:** 3  
**Tags:** graph, pathfinding, priority-queue  
**Created:** 2025-11-30T15:46:35  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def a_star_search(grid, start, goal):
    from heapq import heappop, heappush
    
    rows, cols = len(grid), len(grid[0])
    open_set = []
    heappush(open_set, (0, start))
    came_from = {}
    g_score = {start: 0}
    f_score = {start: abs(start[0] - goal[0]) + abs(start[1] - goal[1])}
    
    while open_set:
        current = heappop(open_set)[1]
        if current == goal:
            path = []
            while current in came_from:
                path.append(current)
                current = came_from[current]
            return path[::-1]
        
        for neighbor in [(current[0]+dx, current[1]+dy) for dx, dy in [(-1,0),(1,0),(0,-1),(0,1)]]:
            if 0 <= neighbor[0] < rows and 0 <= neighbor[1] < cols and grid[neighbor[0]][neighbor[1]] == 0:
                tentative_g_score = g_score[current] + 1
                if tentative_g_score < g_score.get(neighbor, float('inf')):
                    came_from[neighbor] = current
                    g_score[neighbor] = tentative_g_score
                    f_score[neighbor] = tentative_g_score + abs(neighbor[0] - goal[0]) + abs(neighbor[1] - goal[1])
                    if neighbor not in [i[1] for i in open_set]:
                        heappush(open_set, (f_score[neighbor], neighbor))
    return []
```

## Buggy Code

```python
def a_star_search(grid, start, goal):
    from heapq import heappop, heappush
    
    rows, cols = len(grid), len(grid[0])
    open_set = []
    heappush(open_set, (0, start))
    came_from = {}
    g_score = {start: 0}
    f_score = {start: abs(start[0] - goal[0]) + abs(start[1] - goal[1])}
    
    while open_set:
        current = heappop(open_set)[1]
        if current == goal:
            path = []
            while current in came_from:
                path.append(current)
                current = came_from[current]
            return path[::-1]
        
        for neighbor in [(current[0]+dx, current[1]+dy) for dx, dy in [(-1,0),(1,0),(0,-1),(0,1)]]:
            if 0 <= neighbor[0] < rows and 0 <= neighbor[1] < cols and grid[neighbor[0]][neighbor[1]] == 0:
                tentative_g_score = g_score[currnt] + 1
                if tentative_g_score < g_score.get(neighbor, float('inf')):
                    came_from[neighbor] = current
                    g_score[neighbor] = tentative_g_score
                    f_score[neighbor] = tentative_g_score + abs(neighbor[0] - goal[0]) + abs(neighbor[1] - goal[1])
                    if neighbor not in [i[1] for i in open_set]:
                        heappush(open_set, (f_score[neighbor], neighbor))
    return []
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 18 | NameError | Changed 'current' to 'currnt', causing a NameError. |
| 2 | 2 | IndentationError | Removed the indentation of the import statement, causing an IndentationError. |
| 3 | 8 | IndexError | Accessing 'grid[neighbor[0]][neighbor[1]]' with 'neighbor' potentially out of bounds due to the logic. |
