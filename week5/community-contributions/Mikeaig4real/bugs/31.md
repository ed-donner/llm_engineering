# Bug #31

**Level:** medium  
**Description:** implements a stack data structure with push, pop and peek  
**Bug Types:** SyntaxError, IndexError  
**Bug Count:** 2  
**Tags:** stack, data-structure  
**Created:** 2025-07-07T21:58:51  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def stack_operations():
    stack = []
    
    def push(item):
        stack.append(item)
    
    def pop():
        if not stack:
            return None
        return stack.pop()
    
    def peek():
        if not stack:
            return None
        return stack[-1]
    
    return push, pop, peek
```

## Buggy Code

```python
def stack_operations():
    stack = []
    
    def push(item):
        stack.append(item)
    
    def pop():
        if not stack:
            return None
        return stack.pop(
    
    def peek():
        if not stack:
            return None
        return stack[-1]
    
    return push, pop, peek
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 7 | SyntaxError | Missing closing parenthesis in the return statement of the pop function. |
| 2 | 11 | IndexError | Accessing stack[-1] when stack is empty may lead to index error. |
