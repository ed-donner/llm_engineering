# Bug #32

**Level:** medium  
**Description:** determines whether a string is a palindrome using recursion  
**Bug Types:** LogicError  
**Bug Count:** 1  
**Tags:** string, recursion  
**Created:** 2025-07-10T14:55:27  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def is_palindrome(s):
    s = ''.join(filter(str.isalnum, s)).lower()
    if len(s) <= 1:
        return True
    if s[0] != s[-1]:
        return False
    return is_palindrome(s[1:-1])
```

## Buggy Code

```python
def is_palindrome(s):
    s = ''.join(filter(str.isalnum, s)).lower()
    if len(s) < 1:
        return True
    if s[0] != s[-1]:
        return False
    return is_palindrome(s[1:-1])
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | LogicError | Changed condition from len(s) <= 1 to len(s) < 1, which incorrectly returns True for empty string but not for single character. |
