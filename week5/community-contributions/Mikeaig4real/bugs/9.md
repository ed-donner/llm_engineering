# Bug #9

**Level:** easy  
**Description:** checks if a string is a palindrome  
**Bug Types:** IndexError, TypeError  
**Bug Count:** 2  
**Tags:** string, algorithm  
**Created:** 2025-02-19T19:11:44  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def is_palindrome(input_string):
    cleaned_string = ''.join(char.lower() for char in input_string if char.isalnum())
    return cleaned_string == cleaned_string[::-1]
```

## Buggy Code

```python
def is_palindrome(input_string):
    cleaned_string = ''.join(char.lower() for char in input_string if char.isalnum())
    return cleaned_string == cleaned_string[-1:0:-1]
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 3 | IndexError | The slicing used for reversing is incorrect, it should be cleaned_string[::-1] instead of cleaned_string[-1:0:-1, which leads to an IndexError. |
| 2 | 2 | TypeError | If input_string is not a string (for example, None), it will raise a TypeError when trying to iterate over it. |
