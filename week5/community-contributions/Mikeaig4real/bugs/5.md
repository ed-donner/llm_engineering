# Bug #5

**Level:** easy  
**Description:** checks if a number is even or odd  
**Bug Types:** SyntaxError, IndentationError, TypeError  
**Bug Count:** 3  
**Tags:** math, conditional  
**Created:** 2025-02-05T17:50:11  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def check_even_odd(number):
    if not isinstance(number, int):
        return "Input must be an integer."
    if number % 2 == 0:
        return f"{number} is even."
    else:
        return f"{number} is odd."
```

## Buggy Code

```python
def check_even_odd(number):
    if not isinstance(number, int)
        return "Input must be an integer."
   if number % 2 == 0:
        return f"{number} is even."
    else:
        return f"{number} is odd."
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 2 | SyntaxError | Missing colon at the end of the 'if' statement. |
| 2 | 3 | IndentationError | Incorrect indentation level for the return statement. |
| 3 | 4 | TypeError | The function is expected to handle non-integer types, but the return statement does not handle them correctly. |
