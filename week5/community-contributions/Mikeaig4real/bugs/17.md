# Bug #17

**Level:** easy  
**Description:** counts the number of paragraphs in a string  
**Bug Types:** TypeError, IndexError  
**Bug Count:** 2  
**Tags:** string, array  
**Created:** 2025-04-07T08:02:43  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def count_paragraphs(text):
    if not text:
        return 0
    paragraphs = text.splitlines()
    return sum(1 for paragraph in paragraphs if paragraph.strip())
```

## Buggy Code

```python
def count_paragraphs(text):
    if not text:
        return 0
    paragraphss = text.splitlines()
    return sum(1 for paragraph in paragraphss if paragraph.strip())
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 5 | TypeError | Changed 'paragraphs' to 'paragraphss', which leads to a NameError when trying to reference the correct variable. |
| 2 | 6 | IndexError | The logic error in counting paragraphs could lead to an IndexError if 'paragraphss' is empty and accessed incorrectly. |
