# Bug #12

**Level:** easy  
**Description:** removes duplicates from a list  
**Bug Types:** NameError, LogicError  
**Bug Count:** 2  
**Tags:** array, hash-table  
**Created:** 2025-03-13T08:59:51  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def remove_duplicates(input_list):
    if not input_list:
        return []
    unique_items = set()
    result = []
    for item in input_list:
        if item not in unique_items:
            unique_items.add(item)
            result.append(item)
    return result
```

## Buggy Code

```python
def remove_duplicates(input_list):
    if not input_list:
        return []
    unique_item = set()
    result = []
    for item in input_list:
        if item not in unique_items:
            unique_items.add(item)
            result.append(item)
    return result
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 4 | NameError | Changed 'unique_items' to 'unique_item', which causes a NameError when checking for membership. |
| 2 | 7 | LogicError | The 'unique_items' should be 'unique_item' in the if statement, leading to incorrect logic as it will never add items to the result. |
