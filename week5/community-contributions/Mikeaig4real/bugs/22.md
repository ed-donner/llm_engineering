# Bug #22

**Level:** medium  
**Description:** calculates the greatest common divisor (GCD) of two numbers using the Euclidean algorithm  
**Bug Types:** NameError, LogicError, IndentationError  
**Bug Count:** 3  
**Tags:** math, error-handling  
**Created:** 2025-05-26T11:09:52  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def gcd(a, b):
    if a < 0 or b < 0:
        raise ValueError("Both numbers must be non-negative.")
    while b:
        a, b = b, a % b
    return a
```

## Buggy Code

```python
def gcd(a, b):
    if a < 0 or b < 0:
        raise ValueError("Both numbers must be non-negative.")
    while b:
        a, b = b, a % c
    return a

```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 6 | NameError | Changed 'b' to 'c' in 'a % c', causing a NameError since 'c' is not defined. |
| 2 | 2 | LogicError | The logic for checking non-negative numbers is fine, but if we intended to check for non-zero values, it would be flawed. |
| 3 | 5 | IndentationError | Indented the 'while' loop incorrectly, which might lead to an incorrect block structure. |
