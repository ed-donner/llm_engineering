# Bug #52

**Level:** hard  
**Description:** indexes a collection of documents using an inverted index for search efficiency  
**Bug Types:** NameError, LogicError  
**Bug Count:** 2  
**Tags:** hash-table, string  
**Created:** 2025-10-19T05:39:21  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def create_inverted_index(documents):
    inverted_index = {}
    for doc_id, text in enumerate(documents):
        for word in set(text.lower().split()):
            if word not in inverted_index:
                inverted_index[word] = []
            inverted_index[word].append(doc_id)
    return inverted_index
```

## Buggy Code

```python
def create_inverted_index(documents):
    inverted_index = {}
    for doc_id, text in enumerate(document):
        for word in set(text.lower().split()):
            if word not in inverted_index:
                inverted_index[word] = []
            inverted_index[word].apend(doc_id)
    return inverted_index
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 3 | NameError | Changed 'documents' to 'document', causing a NameError due to the undefined variable. |
| 2 | 7 | LogicError | Changed 'append' to 'apend', causing a LogicError as the method call is misspelled and will not execute properly. |
