# Bug #34

**Level:** medium  
**Description:** searches for a target value in an array using linear search algorithm  
**Bug Types:** LogicError  
**Bug Count:** 1  
**Tags:** array, search  
**Created:** 2025-07-22T12:24:13  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def linear_search(array, target):
    if not array:
        return -1
    for index in range(len(array)):
        if array[index] == target:
            return index
    return -1
```

## Buggy Code

```python
def linear_search(array, target):
    if not array:
        return -1
    for index in range(len(array)):
        if array[index] != target:
            return index
    return -1
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 5 | LogicError | Changed '==' to '!=' which causes the function to return the index of the first non-matching element instead of the target. |
