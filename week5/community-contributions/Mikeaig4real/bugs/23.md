# Bug #23

**Level:** medium  
**Description:** finds the longest common subsequence between two strings  
**Bug Types:** IndexError, TypeError  
**Bug Count:** 2  
**Tags:** string, dynamic-programming  
**Created:** 2025-06-05T14:28:15  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def longest_common_subsequence(str1, str2):
    m, n = len(str1), len(str2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if str1[i - 1] == str2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1] + 1
            else:
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])
                
    lcs_length = dp[m][n]
    lcs = []
    while m > 0 and n > 0:
        if str1[m - 1] == str2[n - 1]:
            lcs.append(str1[m - 1])
            m -= 1
            n -= 1
        elif dp[m - 1][n] > dp[m][n - 1]:
            m -= 1
        else:
            n -= 1
            
    return ''.join(reversed(lcs))
```

## Buggy Code

```python
def longest_common_subsequence(str1, str2):
    m, n = len(str1), len(str2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    
    for i in range(1, m + 1):
        for j in range(1, n + 2):
            if str1[i - 1] == str2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1] + 1
            else:
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])
                
    lcs_length = dp[m][n]
    lcs = []
    while m > 0 and n > 0:
        if str1[m - 1] == str2[n - 1]:
            lcs.append(str1[m - 1])
            m -= 1
            n -= 1
        elif dp[m - 1][n] > dp[m][n - 1]:
            m -= 1
        else:
            n -= 1
            
    return ''.join(reversed(lcs))
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 6 | IndexError | Changed 'n + 1' to 'n + 2' in the inner loop, causing potential index out of range access. |
| 2 | 12 | TypeError | The 'n' variable is modified in the while loop, leading to potential invalid index access in 'dp[m][n]' later. |
