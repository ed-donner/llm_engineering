# Bug #42

**Level:** hard  
**Description:** generates all possible permutations of a string  
**Bug Types:** LogicError  
**Bug Count:** 1  
**Tags:** string, recursion  
**Created:** 2025-09-02T09:11:19  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def generate_permutations(s):
    if len(s) <= 1:
        return [s]
    permutations = []
    for i, char in enumerate(s):
        for perm in generate_permutations(s[:i] + s[i+1:]):
            permutations.append(char + perm)
    return permutations
```

## Buggy Code

```python
def generate_permutations(s):
    if len(s) < 1:
        return [s]
    permutations = []
    for i, char in enumerate(s):
        for perm in generate_permutations(s[:i] + s[i+1:]):
            permutations.append(char + perm)
    return permutations
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 3 | LogicError | Changed the condition from 'len(s) <= 1' to 'len(s) < 1', which breaks the base case for single character strings. |
