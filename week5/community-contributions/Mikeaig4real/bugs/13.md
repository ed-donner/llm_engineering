# Bug #13

**Level:** easy  
**Description:** finds the length of the longest word in a string  
**Bug Types:** NameError, IndentationError, SyntaxError  
**Bug Count:** 3  
**Tags:** string, loop, function  
**Created:** 2025-03-16T00:58:12  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def longest_word_length(sentence):
    if not sentence:
        return 0
    words = sentence.split()
    max_length = 0
    for word in words:
        max_length = max(max_length, len(word))
    return max_length
```

## Buggy Code

```python
def longest_word_length(sentence):
    if not sentence:
        return 0
    words = sentence.split()
    max_lenght = 0
    for word in words:
     max_length = max(max_length, len(word))
    return max_length

```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 5 | NameError | Changed 'max_length' to 'max_lenght', causing a reference to an undefined variable. |
| 2 | 6 | IndentationError | Removed one level of indentation before 'max_length = max(max_length, len(word))', leading to incorrect block structure. |
| 3 | 5 | SyntaxError | Missing colon at the end of the for loop declaration. |
