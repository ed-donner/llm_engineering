# Bug #36

**Level:** medium  
**Description:** sorts an array using merge sort algorithm  
**Bug Types:** LogicError, NameError, IndentationError  
**Bug Count:** 3  
**Tags:** sorting, recursion, array  
**Created:** 2025-08-01T16:31:32  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left_half = merge_sort(arr[:mid])
    right_half = merge_sort(arr[mid:])
    return merge(left_half, right_half)

def merge(left, right):
    sorted_array = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] < right[j]:
            sorted_array.append(left[i])
            i += 1
        else:
            sorted_array.append(right[j])
            j += 1
    sorted_array.extend(left[i:])
    sorted_array.extend(right[j:])
    return sorted_array
```

## Buggy Code

```python
def merge_sort(arr):
    if len(arr) < 1:
        return arr
    mid = len(arr) // 2
    left_half = merge_sort(arr[:mid])
    right_half = merge_sort(arr[mid:])
    return merge(left_haf, right_half)

def merge(left, right):
    sorted_array = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] < right[j]:
            sorted_array.append(left[i])
            i += 1
        else:
            sorted_array.append(right[j])
            j += 1
    sorted_array.extend(left[i:])
    sorted_array.extend(right[j:])
    return sorted_array
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 2 | LogicError | Changed '<=' to '<' in the conditional check, which causes an incorrect base case leading to infinite recursion. |
| 2 | 8 | NameError | Misspelled 'left_half' as 'left_haf', causing a NameError when calling the merge function. |
| 3 | 3 | IndentationError | Incorrect indentation in the function body, which leads to a syntax error. |
