# Bug #1

**Level:** easy  
**Description:** converts a string to uppercase  
**Bug Types:** LogicError  
**Bug Count:** 1  
**Tags:** string, algorithm  
**Created:** 2025-01-11T02:00:34  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def convert_to_uppercase(input_string):
    if not isinstance(input_string, str):
        raise ValueError("Input must be a string.")
    if input_string == "":
        return ""
    uppercase_string = ""
    for char in input_string:
        if 'a' <= char <= 'z':
            uppercase_string += chr(ord(char) - 32)
        else:
            uppercase_string += char
    return uppercase_string
```

## Buggy Code

```python
def convert_to_uppercase(input_string):
    if not isinstance(input_string, str):
        raise ValueError("Input must be a string.")
    if input_string == "":
        return ""
    uppercase_string = ""
    for char in input_string:
        if 'a' <= char <= 'z':
            uppercase_string += chr(ord(char) - 31)
        else:
            uppercase_string += char
    return uppercase_string
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 8 | LogicError | The logic for converting lowercase to uppercase is incorrect; it uses -31 instead of -32, which results in wrong character conversion. |
