# Bug #18

**Level:** easy  
**Description:** calculates the sum of all elements in a list  
**Bug Types:** IndentationError, TypeError  
**Bug Count:** 2  
**Tags:** array, math  
**Created:** 2025-04-09T15:07:30  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def sum_of_elements(numbers):
    if not isinstance(numbers, list):
        raise ValueError("Input must be a list")
    if len(numbers) == 0:
        return 0
    total = 0
    for number in numbers:
        if not isinstance(number, (int, float)):
            raise ValueError("All elements in the list must be numbers")
        total += number
    return total
```

## Buggy Code

```python
def sum_of_elements(numbers):
    if not isinstance(numbers, list):
        raise ValueError("Input must be a list")
    if len(numbers) == 0:
        return 0
    total = 0
    for number in numbers:
        if not isinstance(number, (int, float)):
            raise ValueError("All elements in the list must be numbers")
        total += number
    return total
    
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 12 | IndentationError | The return statement is incorrectly indented, which causes it to be considered part of the for loop. |
| 2 | 7 | TypeError | The total variable is being treated as an int but if a string is in the list, it raises TypeError during addition. |
