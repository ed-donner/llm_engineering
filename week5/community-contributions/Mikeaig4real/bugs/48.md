# Bug #48

**Level:** hard  
**Description:** builds a segment tree for efficient range query operations  
**Bug Types:** TypeError  
**Bug Count:** 1  
**Tags:** array, tree  
**Created:** 2025-09-21T16:09:09  
**Model:** `openai/gpt-4o-mini`

## Correct Code

```python
def build_segment_tree(arr):
    n = len(arr)
    tree = [0] * (2 * n)
    for i in range(n):
        tree[n + i] = arr[i]
    for i in range(n - 1, 0, -1):
        tree[i] = tree[i * 2] + tree[i * 2 + 1]
    return tree
```

## Buggy Code

```python
def build_segment_tree(arr):
    n = len(arr)
    tree = [0] * (2 * n)
    for i in range(n):
        tree[n + i] = arr[i]
    for i in range(n - 1, 0, -1):
        tree[i] = tree[i * 2] + tree[i + 1]
    return tree
```

## Bugs Injected

| # | Line | Type | Description |
|---|------|------|-------------|
| 1 | 6 | TypeError | Changed 'tree[i * 2 + 1]' to 'tree[i + 1]' which causes a TypeError since this leads to an incorrect access of the tree array. |
