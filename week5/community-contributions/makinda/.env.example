# Copy to .env at the project root (llm_engineering/.env) or set in your environment.
# OpenRouter (used for LLM; economical for class use)
OPENROUTER_API_KEY=sk-or-xxxxx

# Optional: for HuggingFace gated models (this app uses local all-MiniLM-L6-v2, so HF_TOKEN not required)
# HF_TOKEN=hf_xxxxx
