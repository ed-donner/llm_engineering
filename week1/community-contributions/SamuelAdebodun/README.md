# Technical Question Explainer

Takes a technical question and returns streaming explanations from OpenAI and Ollama. Set `USE_INPUT = True` to type your question or `False` for the built-in example. Needs `.env` with `OPENAI_API_KEY` and Ollama (e.g. `llama3.2`) installed; run cells in order.
