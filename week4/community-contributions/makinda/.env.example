# We use OpenRouter for API access (including OpenAI models). One key for OpenRouter + OpenAI.
OPENROUTER_API_KEY=sk-or-xxxxx

# Optional: Gemini (native); Ollama uses no key (run ollama serve locally)
# GOOGLE_API_KEY=AIzaxxxxx
