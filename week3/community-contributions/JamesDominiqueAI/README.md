ResumeMatch uses AI to evaluate how well a resume fits a job description.
It includes 5 built-in job descriptions and allows users to add custom ones. 
The system highlights matched, missing, and bonus skills and can rank multiple candidates for the same role.