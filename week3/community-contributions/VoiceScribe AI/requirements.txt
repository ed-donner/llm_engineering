# VoiceScribe AI - Requirements

# Core dependencies
gradio>=4.0.0
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0

# Audio processing (librosa handles audio loading without needing system ffmpeg)
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.1
numba>=0.56.0  # Required by librosa

# RAG and Vector Store (with compatible versions to avoid conflicts)
langchain-core>=0.3.72,<1.0.0  # IMPORTANT: Must be < 1.0.0 for compatibility
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0
langchain-huggingface>=0.0.1,<1.0.0  # Must be < 1.0.0 to match langchain-core
faiss-cpu>=1.7.4  # Use faiss-gpu if you have CUDA
sentence-transformers>=2.2.2

# Utilities
numpy>=1.24.0
pandas>=2.0.0

# Optional: For GPU support
# faiss-gpu>=1.7.4  # Uncomment if using CUDA
